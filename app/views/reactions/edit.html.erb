<div class="wrapper container">
   
  <% content_for(:title, "Editing #{@reaction.name}") %>

  <h1>Editing <%= @reaction.name %></h1>

  <%= render 'form' %>

  <%= link_to 'Show', @reaction %> |
  <%= link_to 'Back', reactions_path %>

</div> <!-- wrapper container -->

