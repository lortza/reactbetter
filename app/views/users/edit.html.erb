<% content_for(:title, "Account Settings for #{@user.name}") %>

<div class="container wrapper">
  <div class="pull-right">
    <%= link_to ' Cancel', users_path, class: "btn btn-primary btn-sm fa fa-ban" %>  
  </div> <!-- pull-right -->
     
  <h1>Account Settings</h1>

  <%= render "form" %>

</div> <!-- container wrapper -->
